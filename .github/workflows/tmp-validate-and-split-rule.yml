name: Tmp Validate And Split Rule

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate candidate and-split rule against real bill sentences
        run: node scripts/tmp-validate-and-split-rule.js

      - name: Commit results file
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add tmp-validation-run1-results.txt
          git diff --staged --quiet || git commit -m "Add validation run 1 results for review"
          git pull --rebase
          git push origin HEAD
